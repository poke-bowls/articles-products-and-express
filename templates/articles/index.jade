extends ./../layout

block content

  h1 These are our articles
  ul
    each article in articles
      li
        a(href="/articles") Grab #{article.title} by #{article.author} now! Only #{article.body} copies left!